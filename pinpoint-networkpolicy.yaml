apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: pinpoint
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/pinpoint: "true"
  podSelector:
    matchLabels:
      io.kompose.network/pinpoint: "true"
